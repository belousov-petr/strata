# Reference — {{PROJECT_NAME}}

Stable facts you look up: paths, schemas, API surfaces, naming conventions, external system quirks. One topic per `<slug>.md`. The discriminator: a *fact* lives here; a *procedure* (steps you execute) lives in `../ops/`; a *rule* that changes agent behavior lives in `.strata/memory/learnings/`.

Rule of thumb: the second time you look something up, write it down here.
