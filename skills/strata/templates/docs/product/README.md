# Product — {{PROJECT_NAME}}

PRDs and product requirements, one per `<slug>.md`: what should exist, for whom, and what "done" means. The *why-build-it* layer — how it works belongs in `../architecture/`, why it was decided this way in `../decisions/`.
