# Transient working-tree clutter — not part of the plugin.

# Windows download metadata
*:Zone.Identifier

# Loose working images / screenshots (the committed hero image is Strata.png)
Strata_old.png
*Playground*.png
Screenshot*.png

# Local review / shakedown reports (generated by the shakedown skill, not plugin content)
/issues/

# Subagent-driven-development / superpowers scratch
.superpowers/

# Private strata dogfood memory — kept local in WSL, not shipped in this public plugin repo
.strata/
