# Architecture specs — {{PROJECT_NAME}}

Deep specs, one topic per `<slug>.md`, indexed from `../ARCHITECTURE.md`. Checklist of sections worth having (write what applies, skip the rest): solution overview · constraints · context & scope · data flow · tool surfaces (commands, MCP, APIs) · prompt/skill design · state & memory · observability · risks & debt.

Exhaustive is fine here — this tier loads on demand only.
